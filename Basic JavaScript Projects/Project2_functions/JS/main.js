function concatenate() {
    var sentence = "This is "; //assign variable
    sentence += "Javascript!" // add another text using += operators
    document.getElementById("concat").innerHTML = sentence // show concatenated text in paragraph
}